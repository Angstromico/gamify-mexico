---
import DefaultLayout from "@layouts/DefaultLayout.astro"
import RegistrationForm from '@components/Signup'
---
<DefaultLayout image="/sign.png" title="Pagina de Registro de Gamify Mexico" description="Pagina de Registro de Gamify Mexico">
<RegistrationForm
        title="Registrese"
        nameLabel="Nombre"
        emailLabel="Email"
        passwordLabel="Clave"
        confirmPasswordLabel="Confirmar Clave"
        phoneLabel="Telefono"
        submitButtonText="Registro"
        loginText="Ya tienes cuenta? Entra"
        loginLink="/login"
        namePlaceholder="Tu nombre"
        emailPlaceholder="tu@ejemplo.com"
        passwordPlaceholder="********"
        confirmPasswordPlaceholder="********"
        phonePlaceholder="Tu telefono"
        GEO_API={import.meta.env.GEO_API}
        API={import.meta.env.API}
        client:load
      />
</DefaultLayout>

